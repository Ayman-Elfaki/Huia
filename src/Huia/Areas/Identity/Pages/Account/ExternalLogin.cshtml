@page
@model ExternalLoginModel
@{
    // Handler-only page: OnPostAsync challenges the provider, OnGetCallbackAsync completes and redirects
    // away, and a bare GET (OnGet) redirects to Login — there's nothing for this page to actually render.
}
