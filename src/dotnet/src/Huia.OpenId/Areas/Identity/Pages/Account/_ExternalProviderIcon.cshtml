@model (Huia.Options.ExternalProviderKind Kind, string PathBase)
@{
    var slug = Model.Kind switch
    {
        Huia.Options.ExternalProviderKind.Google => "google",
        Huia.Options.ExternalProviderKind.GitHub => "github",
        Huia.Options.ExternalProviderKind.MicrosoftAccount => "microsoft",
        _ => "openid",
    };
}
<span class="huia-provider-icon huia-provider-@slug" aria-hidden="true"></span>
